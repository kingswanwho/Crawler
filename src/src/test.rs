#[cfg(test)]
mod test_site {
	use crawler;
	use hyper::Url;
	use crawler::Site;

	// #[test]
	// fn test_new_case() {
	// 	let url = Url::parse("https//:www.google.com").unwrap();
	// 	let result = crawler::Site::new(url);
	// 	let url_test = Url::parse("https//:www.google.com").unwrap();
	// 	let vec = Vec::new();
	// 	let site = Site {
	// 		url_test,
	// 		vec,
	// 	};
	// 	assert_eq!(site, result);
	// }

}